# @rebook/web

React 18 + Vite + TypeScript + Ant Design 5 + Tailwind CSS + TanStack Query + Zustand。

## Local dev

```bash
cp .env.example .env
# 在仓库根目录
pnpm install
pnpm dev:web
```

打开 http://localhost:5173 ，登录账号见后端 README（`admin` / `password123`）。

## 添加新功能

请遵循 `.cursor/skills/web-page`。
